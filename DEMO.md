# 上司向けデモ手順書

目的: 「声の記憶帳」の成果を見せる。(1)いつでも聴ける公開ページ、(2)目の前で録音→生成するライブデモの2本立て。

## いつでも見せられるもの(常設)

- **公開試聴室**: https://somanakahashi-ops.github.io/Voice-Cloning/
  - 開くと試聴室タブが最初に表示され、方式聴き比べ4本+変換済み章音声2本がその場で再生できる
  - PCの起動状態と無関係に24時間動く。URLを渡すだけでよい
  - 生成機能は公開ページでは動かない(音声は事前生成したもの)

## ライブデモ(対面): 上司の声を「声紋」にする

一番刺さる流れ。その場で録音した上司の声を声紋登録し、既存の章を**上司自身の声**で読み上げる。

### 事前準備(前日までに)

1. PCが起動していればバックエンドは常駐済み。ブラウザで http://localhost:8000/app/ が開くことを確認
2. 記憶帳タブに章(本人録音の下書き登録済み)があることを確認。なければ作り直す:
   - 章を作成 → 本人録音ファイルをアップロード(m4a等そのままでOK、自動変換される)
3. **リハーサルを必ず1回通す**(下の当日の流れをそのまま)。初回はモデルロードが入るぶん遅い(+30秒程度)ので、当日の直前に一度なにか変換しておくと本番が速い
4. 録音手段の確認: スマホのボイスメモ or Windowsボイスレコーダー。ファイルをPCに渡す方法(USB/LINE/メール)も確認

### 当日の流れ(所要 約10分)

1. 公開試聴室を見せながらプロジェクト概要を説明(2分)
2. 上司に台本を渡し、30秒ほど読み上げてもらって録音(スマホ推奨)(2分)
   - 台本は自然な話し言葉の文章なら何でもよい。母音だけ・単語だけより連続発話が良い
3. 録音ファイルをPCへ → 記憶帳タブで**声紋として登録**(名前:「◯◯さんの声」)(1分)
4. 既存の章に上司の声紋を適用 → 生成開始(操作10秒)
5. **待ち時間 約1〜2分**: ここで仕組み(2段階構成・声紋の意味・CPUだけで動いていること)を説明。処理時間を体感してもらうのも狙いのうち
6. 完成 → 上司自身の声で章が再生される

### 注意・保険

- 生成は**同時に1件だけ**(並行するとメモリ不足で落ちる)
- ライブが失敗しても公開試聴室に事前生成の成果があるので、成果ゼロにはならない
- サーバーが応答しない場合: タスクスケジューラの `VoiceCloningBackend` を再実行(登録済みデータはstate.jsonに永続化されているので消えない)
- 録音した上司の声は本人録音と同じ非公開領域(storage_private または storage/voice_profiles)に置かれ、リポジトリにはpushされない
