# 上司向けデモ手順書

目的: 「声の記憶帳」の成果を見せる。(1)いつでも聴ける公開ページ、(2)目の前で録音→生成するライブデモの2本立て。

## いつでも見せられるもの(常設)

- **公開試聴室**: https://somanakahashi-ops.github.io/Voice-Cloning/
  - 開くと試聴室タブが最初に表示され、方式聴き比べ4本+変換済み章音声2本がその場で再生できる
  - PCの起動状態と無関係に24時間動く。URLを渡すだけでよい
  - 生成機能は公開ページでは動かない(音声は事前生成したもの)

## ライブデモ(対面): 上司の声を「声紋」にする

一番刺さる流れ。その場で録音した上司の声を声紋登録し、既存の章を**上司自身の声**で読み上げる。

### 事前準備(前日までに)

1. バックエンドは**常駐させていない**(PC起動中ずっと動かすのは無駄なため、使うときだけ起動する方式)。デスクトップの「**声の記憶帳を起動.bat**」をダブルクリックすると、裏でバックエンドが立ち上がり、数秒後に自動でブラウザが http://localhost:8000/app/ を開く
2. 記憶帳タブに「第一章 誕生」と「声紋A(研究サンプル)」が登録済みであることを確認(2026-07-15に登録済み。state.jsonに永続化されるので、バックエンドを止めても消えない)
3. **リハーサルを必ず1回通す**(下の当日の流れをそのまま)。初回はモデルのダウンロード/ロードが入るぶん遅いので、当日の直前に一度なにか生成しておくと本番が速い
4. マイクの確認: PCにマイクがあれば**アプリ内の録音ボタンでそのまま録れる**(ブラウザのマイク許可を事前に一度出しておく)。なければスマホ録音→ファイルをPCへ(m4a等そのままアップロード可)
5. デモが終わったら特に何もしなくてよい(次回起動時にまた同じbatを叩くだけ)。手動で止めたい場合はタスクスケジューラで `VoiceCloningBackend` のプロセスを終了するか、PCを再起動すれば消える

### 当日の流れ(所要 約10〜15分)

1. 公開試聴室を見せながらプロジェクト概要を説明(2分)
2. 上司に30秒ほど自由に話してもらい録音(2分)
   - 記憶帳タブ「声紋を録音する」でアプリ内録音(推奨)、またはファイルアップロード
   - 自然な話し言葉がよい(母音だけ・単語だけより連続発話)
   - そのまま**声紋として登録**(名前:「◯◯さんの声」)
3. デモは2パターンある。時間があれば両方:
   - **パターンA(テキストだけ)**: 章の本文テキストを入力 → Step2で上司の声紋を選んで「この声で仕上げる」→ **録音した本人が読んでいない文章を、上司の声が読み上げる**(2文で約2〜4分)
   - **パターンB(録音を変換)**: 既存の「第一章」(本人録音の下書き付き)に上司の声紋を適用 → 読み上げの抑揚はそのままに声質だけ上司になる(約1〜2分)
4. **待ち時間**: 仕組み(声紋の意味・ワンショット生成と声質変換の違い・CPUだけで動いていること)を説明。処理時間を体感してもらうのも狙いのうち
5. 完成 → 上司自身の声で再生される

### 注意・保険

- 生成は**同時に1件だけ**押す(サーバー側でも直列化してあるが、複数積むと待ち時間が読めなくなる)
- ライブが失敗しても公開試聴室に事前生成の成果があるので、成果ゼロにはならない
- サーバーが応答しない場合: タスクスケジューラの `VoiceCloningBackend` を再実行(登録済みデータはstate.jsonに永続化されているので消えない)
- 録音した上司の声(声紋・下書きとも)はローカルにのみ保存され、リポジトリにはpushされない(音声ファイルは.gitignoreで除外)
